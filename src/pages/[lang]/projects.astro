---
import ProjectsPage from "../../components/pages/Projects.astro";

export function getStaticPaths() {
    return [{ params: { lang: "en" } }];
}

const { lang } = Astro.params;
---

<ProjectsPage lang={lang as "en" | "tr"} />
